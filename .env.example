# Production env vars for Docker build.
# At build time these are set to placeholders (md5 of key name).
# At runtime (e.g. AWS) real values are injected by search_and_replace_env.js.
# Add any NEXT_PUBLIC_* or other vars that Next.js inlines at build time.

NEXT_PUBLIC_API_BASE_URL=https://api.example.com
NEXT_PUBLIC_AUTH_REFRESH_PATH=/api/auth/refresh
