# E2E test configuration. Copy to .env.e2e and fill in (do not commit .env.e2e).
# In GitHub Actions, set these as repository secrets and pass them as env to the job.

# App URL under test
CYPRESS_BASE_URL=http://localhost:3000

# Backend API base URL (for admin OTP task and setTestUserEmail)
CYPRESS_API_BASE_URL=https://api.dev.itamba.xyz

# Admin credentials (for getOtpByRequestId in phone signup test)
CYPRESS_ADMIN_EMAIL=vanisiah@iknite.studio
CYPRESS_ADMIN_PASSWORD=vanisiah@iknite.studio

# Optional: test user inputs
# CYPRESS_TEST_PHONE=677000001
# CYPRESS_TEST_PASSWORD=TestPass1

# For browse-only and subscription-only specs (existing user)
# CYPRESS_TEST_USER_EMAIL=e2e-test+xxx@test.itamba.local
